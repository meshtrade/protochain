// @generated
// This file is @generated by prost-build.
/// TokenProgram identifies which Solana token program to use for an operation.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum TokenProgram {
    /// UNSPECIFIED is invalid and must not be used. Services will return an error if this value is received.
    Unspecified = 0,
    /// SPL refers to the original SPL Token program (spl-token), also known as the legacy token program.
    /// Most existing tokens use this program.
    Spl = 1,
    /// 2022 refers to the Token Extensions program (spl-token-2022), also known as Token-2022.
    /// Supports additional features such as transfer fees, interest-bearing tokens, and more.
    TokenProgram2022 = 2,
}
impl TokenProgram {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            TokenProgram::Unspecified => "TOKEN_PROGRAM_UNSPECIFIED",
            TokenProgram::Spl => "TOKEN_PROGRAM_SPL",
            TokenProgram::TokenProgram2022 => "TOKEN_PROGRAM_2022",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "TOKEN_PROGRAM_UNSPECIFIED" => Some(Self::Unspecified),
            "TOKEN_PROGRAM_SPL" => Some(Self::Spl),
            "TOKEN_PROGRAM_2022" => Some(Self::TokenProgram2022),
            _ => None,
        }
    }
}
/// Configuration for the Token-2022 Memo Transfer extension on a holding account.
///
/// When enabled, every inbound SPL token transfer into the account must include
/// a memo instruction in the same transaction. Transfers without a memo will be
/// rejected by the Token-2022 program at runtime.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct MemoTransferConfig {
    /// When true, require every inbound transfer into the account to include a memo.
    #[prost(bool, tag="1")]
    pub require_incoming_memo: bool,
}
/// Corresponds to the Metaplex DataV2 Rust struct used for on-chain metadata.
/// See: <https://github.com/metaplex-foundation/mpl-token-metadata>
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MetaplexTokenMetadata {
    /// The full name of the token (e.g. "USD Coin").
    #[prost(string, tag="1")]
    pub name: ::prost::alloc::string::String,
    /// The ticker/symbol of the token (e.g. "USDC").
    #[prost(string, tag="2")]
    pub symbol: ::prost::alloc::string::String,
    /// URL pointing to an off-chain JSON file (Arweave, IPFS, or HTTPS).
    /// Follows the Metaplex standard for displaying images and descriptions in wallets.
    /// See: <https://developers.metaplex.com/smart-contracts/token-metadata/token-standard>
    #[prost(string, tag="3")]
    pub uri: ::prost::alloc::string::String,
    /// Royalties in basis points (100 = 1%).
    #[prost(uint32, tag="4")]
    pub seller_fee_basis_points: u32,
    /// List of creators and their royalty shares.
    #[prost(message, repeated, tag="5")]
    pub creators: ::prost::alloc::vec::Vec<MetaplexCreator>,
    /// Optional collection information grouping this asset.
    #[prost(message, optional, tag="6")]
    pub collection: ::core::option::Option<MetaplexCollection>,
    /// Optional usage restrictions/rules (primarily for gaming or limited-use assets).
    #[prost(message, optional, tag="7")]
    pub uses: ::core::option::Option<MetaplexUses>,
}
/// Creator information for royalties.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MetaplexCreator {
    /// The public key of the creator (Base58 encoded string).
    #[prost(string, tag="1")]
    pub address: ::prost::alloc::string::String,
    /// Whether the creator has signed the metadata (verified).
    #[prost(bool, tag="2")]
    pub verified: bool,
    /// The percentage of royalties this creator receives (0-100).
    #[prost(uint32, tag="3")]
    pub share: u32,
}
/// Collection grouping info.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MetaplexCollection {
    /// Whether the collection has been verified by the authority.
    #[prost(bool, tag="1")]
    pub verified: bool,
    /// The mint address of the collection parent NFT (Base58 encoded string).
    #[prost(string, tag="2")]
    pub key: ::prost::alloc::string::String,
}
/// Usage rules for the asset.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct MetaplexUses {
    /// How the asset is consumed.
    #[prost(enumeration="metaplex_uses::UseMethod", tag="1")]
    pub use_method: i32,
    /// Remaining number of uses.
    #[prost(uint64, tag="2")]
    pub remaining: u64,
    /// Total allowed uses.
    #[prost(uint64, tag="3")]
    pub total: u64,
}
/// Nested message and enum types in `MetaplexUses`.
pub mod metaplex_uses {
    #[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
    #[repr(i32)]
    pub enum UseMethod {
        /// Default unspecified value.
        Unspecified = 0,
        /// The asset is burned after use.
        Burn = 1,
        /// The asset can be used multiple times.
        Multiple = 2,
        /// The asset can be used once.
        Single = 3,
    }
    impl UseMethod {
        /// String value of the enum field names used in the ProtoBuf definition.
        ///
        /// The values are not transformed in any way and thus are considered stable
        /// (if the ProtoBuf definition does not change) and safe for programmatic use.
        pub fn as_str_name(&self) -> &'static str {
            match self {
                UseMethod::Unspecified => "USE_METHOD_UNSPECIFIED",
                UseMethod::Burn => "USE_METHOD_BURN",
                UseMethod::Multiple => "USE_METHOD_MULTIPLE",
                UseMethod::Single => "USE_METHOD_SINGLE",
            }
        }
        /// Creates an enum from field names used in the ProtoBuf definition.
        pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
            match value {
                "USE_METHOD_UNSPECIFIED" => Some(Self::Unspecified),
                "USE_METHOD_BURN" => Some(Self::Burn),
                "USE_METHOD_MULTIPLE" => Some(Self::Multiple),
                "USE_METHOD_SINGLE" => Some(Self::Single),
                _ => None,
            }
        }
    }
}
/// Configuration for Token-2022 Metadata Extension
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionMetadata {
    /// The account that serves as the "source of truth" for metadata.
    /// For the most common modern setup, this can be left empty to default
    /// to the mint_pub_key.
    #[prost(string, tag="1")]
    pub metadata_address: ::prost::alloc::string::String,
    /// The authority allowed to sign and update the metadata fields
    /// (name, symbol, uri, etc).
    /// If not provided, this defaults to the mint_authority_pub_key.
    #[prost(string, tag="2")]
    pub update_authority_pub_key: ::prost::alloc::string::String,
    /// The full name of the token (e.g. "USD Coin").
    #[prost(string, tag="3")]
    pub name: ::prost::alloc::string::String,
    /// The ticker/symbol of the token (e.g. "USDC").
    #[prost(string, tag="4")]
    pub symbol: ::prost::alloc::string::String,
    /// URL pointing to an off-chain JSON file (IPFS/Arweave/HTTPS).
    /// Follows the Metaplex standard for displaying images and descriptions in wallets.
    /// See: <https://developers.metaplex.com/smart-contracts/token-metadata/token-standard>
    #[prost(string, tag="5")]
    pub uri: ::prost::alloc::string::String,
    /// Custom key-value pairs stored directly on the blockchain.
    /// Use this for data that smart contracts need to read (e.g., "rarity": "mythic").
    /// Note: Adding many items increases the rent cost of the Mint account.
    #[prost(map="string, string", tag="6")]
    pub additional_metadata: ::std::collections::HashMap<::prost::alloc::string::String, ::prost::alloc::string::String>,
}
/// Configuration for the Token-2022 Mint Close Authority extension.
///
/// When enabled, allows the designated authority to close the mint account and
/// reclaim its rent-exempt lamports. Without this extension a mint account can
/// never be closed.
///
/// The close authority must be set at mint creation time — it cannot be added
/// after initialisation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionMintCloseAuthority {
    /// The public key of the authority that may close the mint account.
    /// If empty, defaults to the mint authority.
    #[prost(string, tag="1")]
    pub close_authority_pub_key: ::prost::alloc::string::String,
}
/// Configuration for the Token-2022 Transfer Fee extension.
///
/// When enabled, a fee is assessed on every token transfer. The fee is
/// expressed in basis points (hundredths of a percent) of the transfer amount,
/// capped at a maximum fee. Fees are withheld in the destination token account
/// and can be harvested by the withdraw authority.
///
/// Must be configured at mint creation time.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionTransferFee {
    /// The authority that can modify the transfer fee parameters.
    /// If empty, no authority is set and the fee configuration is immutable.
    #[prost(string, tag="1")]
    pub transfer_fee_config_authority_pub_key: ::prost::alloc::string::String,
    /// The authority that can withdraw withheld fees from token accounts.
    /// If empty, no withdraw authority is set.
    #[prost(string, tag="2")]
    pub withdraw_withheld_authority_pub_key: ::prost::alloc::string::String,
    /// Fee assessed on each transfer, in basis points of the transfer amount.
    /// One basis point = 0.01%. For example: 100 = 1%, 250 = 2.5%, 10000 = 100%.
    /// Maximum value: 10000.
    #[prost(uint32, tag="3")]
    pub transfer_fee_basis_points: u32,
    /// Maximum fee assessed on any single transfer, in base token units.
    /// For example, with 6 decimals: "1000000" = 1.0 token maximum fee.
    /// Use "0" for no maximum (fee is always calculated from basis points).
    #[prost(uint64, tag="4")]
    pub maximum_fee: u64,
}
/// Configuration for the Token-2022 Default Account State extension.
///
/// When enabled, all new token accounts for this mint are created in the
/// specified default state. This is commonly used with `FROZEN` to require
/// the freeze authority to explicitly approve each holder before they can
/// transact.
///
/// Requires the mint to have a freeze authority set.
/// Must be configured at mint creation time.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionDefaultAccountState {
    /// The default state for new token accounts.
    #[prost(enumeration="AccountState", tag="1")]
    pub state: i32,
}
/// Possible states for a token account.
#[derive(Clone, Copy, Debug, PartialEq, Eq, Hash, PartialOrd, Ord, ::prost::Enumeration)]
#[repr(i32)]
pub enum AccountState {
    /// Unspecified / default — should not be used explicitly.
    Unspecified = 0,
    /// Accounts are created in the normal initialized state (default behaviour
    /// when this extension is not present).
    Initialized = 1,
    /// Accounts are created in the frozen state. The freeze authority must
    /// explicitly thaw each account before the owner can transact.
    Frozen = 2,
}
impl AccountState {
    /// String value of the enum field names used in the ProtoBuf definition.
    ///
    /// The values are not transformed in any way and thus are considered stable
    /// (if the ProtoBuf definition does not change) and safe for programmatic use.
    pub fn as_str_name(&self) -> &'static str {
        match self {
            AccountState::Unspecified => "ACCOUNT_STATE_UNSPECIFIED",
            AccountState::Initialized => "ACCOUNT_STATE_INITIALIZED",
            AccountState::Frozen => "ACCOUNT_STATE_FROZEN",
        }
    }
    /// Creates an enum from field names used in the ProtoBuf definition.
    pub fn from_str_name(value: &str) -> ::core::option::Option<Self> {
        match value {
            "ACCOUNT_STATE_UNSPECIFIED" => Some(Self::Unspecified),
            "ACCOUNT_STATE_INITIALIZED" => Some(Self::Initialized),
            "ACCOUNT_STATE_FROZEN" => Some(Self::Frozen),
            _ => None,
        }
    }
}
/// Configuration for the Token-2022 Permanent Delegate extension.
///
/// When enabled, grants an irrevocable delegate authority over ALL token
/// accounts for this mint. The permanent delegate can transfer or burn tokens
/// from any holder's account without the holder's consent.
///
/// Use cases include regulated assets (compliance seizure), subscription
/// tokens, and revocable access tokens.
///
/// Must be configured at mint creation time. Cannot be changed after
/// initialisation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionPermanentDelegate {
    /// The public key of the permanent delegate that has transfer and burn
    /// authority over all token accounts for this mint.
    #[prost(string, tag="1")]
    pub delegate_pub_key: ::prost::alloc::string::String,
}
/// Configuration for the Token-2022 Pausable extension.
///
/// When enabled, the designated pause authority can halt all minting, burning,
/// and transferring of tokens for this mint. While paused, no token operations
/// can be performed until the authority resumes activity.
///
/// Use cases include emergency circuit breakers, regulatory compliance holds,
/// and migration freezes.
///
/// Must be configured at mint creation time.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionPausable {
    /// The public key of the authority that can pause and resume activity on
    /// the mint. If empty, defaults to the mint authority.
    #[prost(string, tag="1")]
    pub authority_pub_key: ::prost::alloc::string::String,
}
/// Wrapper for Token-2022 mint extensions.
///
/// Each extension is initialised at mint creation time via CreateToken2022Mint.
/// Extensions cannot be added after the mint account has been created.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct Token2022Extension {
    #[prost(oneof="token2022_extension::Extension", tags="1, 2, 3, 4, 5, 6")]
    pub extension: ::core::option::Option<token2022_extension::Extension>,
}
/// Nested message and enum types in `Token2022Extension`.
pub mod token2022_extension {
    #[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Oneof)]
    pub enum Extension {
        /// Token Metadata extension — stores name, symbol, URI and custom
        /// key-value pairs directly on the mint account.
        #[prost(message, tag="1")]
        Metadata(super::Token2022ExtensionMetadata),
        /// Mint Close Authority extension — allows the designated authority to
        /// close the mint account and reclaim its rent-exempt lamports.
        #[prost(message, tag="2")]
        MintCloseAuthority(super::Token2022ExtensionMintCloseAuthority),
        /// Transfer Fee extension — assesses a fee (in basis points) on every
        /// token transfer, withheld in the destination account for later
        /// collection.
        #[prost(message, tag="3")]
        TransferFee(super::Token2022ExtensionTransferFee),
        /// Default Account State extension — new token accounts for this mint
        /// are created in the specified state (e.g. Frozen).
        #[prost(message, tag="4")]
        DefaultAccountState(super::Token2022ExtensionDefaultAccountState),
        /// Permanent Delegate extension — grants an irrevocable delegate
        /// authority over all token accounts for this mint.
        #[prost(message, tag="5")]
        PermanentDelegate(super::Token2022ExtensionPermanentDelegate),
        /// Pausable extension — allows the pause authority to halt all minting,
        /// burning, and transferring for this mint.
        #[prost(message, tag="6")]
        Pausable(super::Token2022ExtensionPausable),
    }
}
/// Configuration for the Token-2022 Immutable Owner extension on a holding
/// account.
///
/// When enabled, the token account's owner cannot be changed via the
/// `SetAuthority` instruction. This provides stronger ownership guarantees
/// and is automatically enabled for all Associated Token Accounts (ATAs)
/// created via the ATA program under Token-2022.
///
/// This extension has no configuration fields — its presence alone activates
/// the restriction.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct Token2022ExtensionImmutableOwner {
}
/// Wrapper for Token-2022 extensions that can be enabled on a holding account
/// (Associated Token Account).
///
/// Each extension must be requested at account creation time via the
/// CreateToken2022HoldingAccount RPC. Extensions not included during creation
/// cannot be added later without reallocating the account.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Message)]
pub struct Token2022HoldingAccountExtension {
    #[prost(oneof="token2022_holding_account_extension::Extension", tags="1, 2")]
    pub extension: ::core::option::Option<token2022_holding_account_extension::Extension>,
}
/// Nested message and enum types in `Token2022HoldingAccountExtension`.
pub mod token2022_holding_account_extension {
    #[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, Copy, PartialEq, ::prost::Oneof)]
    pub enum Extension {
        /// Memo Transfer extension: requires every inbound transfer to include a memo.
        #[prost(message, tag="1")]
        MemoTransfer(super::MemoTransferConfig),
        /// Immutable Owner extension: prevents the token account's owner from
        /// being changed. Automatically enabled for ATAs under Token-2022, but
        /// can be explicitly requested for clarity.
        #[prost(message, tag="2")]
        ImmutableOwner(super::Token2022ExtensionImmutableOwner),
    }
}
/// Request to create and fully initialise a Token-2022 mint account.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateToken2022MintRequest {
    /// The public key of the account that will pay for account creation (signer).
    #[prost(string, tag="1")]
    pub payer_pub_key: ::prost::alloc::string::String,
    /// The public key of the Mint account to be created and initialized (signer).
    #[prost(string, tag="2")]
    pub mint_pub_key: ::prost::alloc::string::String,
    /// The authority that can mint new tokens.
    #[prost(string, tag="3")]
    pub mint_authority_pub_key: ::prost::alloc::string::String,
    /// Optional authority that can freeze token accounts.
    /// Leave empty to disable freeze functionality.
    #[prost(string, tag="4")]
    pub freeze_authority_pub_key: ::prost::alloc::string::String,
    /// Number of base 10 digits to the right of the decimal place.
    /// Common values: 9 for SOL-like tokens, 6 for USDC-like tokens, 0 for NFTs.
    #[prost(uint32, tag="5")]
    pub decimals: u32,
    /// Ordered list of extensions to enable on the mint.
    /// NOTE: If extensions are not included here during initialization,
    /// they cannot be added to the mint account later.
    #[prost(message, repeated, tag="6")]
    pub extensions: ::prost::alloc::vec::Vec<Token2022Extension>,
}
/// Response containing the complete instruction set for Token-2022 mint creation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateToken2022MintResponse {
    /// Ordered instructions to include in a transaction.
    /// Includes System::CreateAccount followed by all Token-2022 initialisation instructions.
    #[prost(message, repeated, tag="1")]
    pub instructions: ::prost::alloc::vec::Vec<super::super::super::transaction::v1::SolanaInstruction>,
    /// Lamports deposited into the mint account for rent exemption.
    /// Covers the full final account size including variable-length extension data
    /// (e.g. token metadata) that Token-2022 allocates via realloc after creation.
    #[prost(uint64, tag="2")]
    pub lamports: u64,
    /// Initial account space in bytes passed to System::CreateAccount.
    /// Covers the base mint layout and fixed-size extension pods only.
    /// Token-2022 may realloc beyond this size for variable-length extensions.
    #[prost(uint64, tag="3")]
    pub space: u64,
}
/// Request to create and fully initialise a legacy SPL Token mint account.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateSplTokenMintRequest {
    /// The public key of the account that will pay for account creation (signer).
    #[prost(string, tag="1")]
    pub payer_pub_key: ::prost::alloc::string::String,
    /// The public key of the Mint account to be created and initialized (signer).
    #[prost(string, tag="2")]
    pub mint_pub_key: ::prost::alloc::string::String,
    /// The authority that can mint new tokens.
    #[prost(string, tag="3")]
    pub mint_authority_pub_key: ::prost::alloc::string::String,
    /// Optional authority that can freeze token accounts.
    /// Leave empty to disable freeze functionality.
    #[prost(string, tag="4")]
    pub freeze_authority_pub_key: ::prost::alloc::string::String,
    /// Number of base 10 digits to the right of the decimal place.
    /// Common values: 9 for SOL-like tokens, 6 for USDC-like tokens, 0 for NFTs.
    #[prost(uint32, tag="5")]
    pub decimals: u32,
    /// Optional Metaplex metadata to attach to the mint via the Metaplex Token Metadata program.
    /// When provided, a CreateMetadataAccountV3 instruction is appended after initialize_mint.
    #[prost(message, optional, tag="6")]
    pub metadata: ::core::option::Option<MetaplexTokenMetadata>,
}
/// Response containing the complete instruction set for SPL Token mint creation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateSplTokenMintResponse {
    /// Ordered instructions to include in a transaction.
    /// Includes System::CreateAccount, initialize_mint, and optionally CreateMetadataAccountV3.
    #[prost(message, repeated, tag="1")]
    pub instructions: ::prost::alloc::vec::Vec<super::super::super::transaction::v1::SolanaInstruction>,
    /// Lamports deposited into the mint account for rent exemption.
    #[prost(uint64, tag="2")]
    pub lamports: u64,
    /// Account space in bytes passed to System::CreateAccount (always 82 bytes for SPL Token mints).
    #[prost(uint64, tag="3")]
    pub space: u64,
}
/// Request to parse mint account
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ParseMintRequest {
    #[prost(string, tag="1")]
    pub account_address: ::prost::alloc::string::String,
}
/// Response with parsed mint data.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct ParseMintResponse {
    /// Base mint account fields (authority, decimals, supply, etc.).
    #[prost(message, optional, tag="1")]
    pub mint: ::core::option::Option<MintInfo>,
    /// The token program that owns this mint account (Legacy SPL Token or Token-2022).
    #[prost(enumeration="TokenProgram", tag="2")]
    pub token_program: i32,
    /// Extensions present on the mint account.
    /// Only populated for Token-2022 mints that have extensions configured.
    #[prost(message, repeated, tag="3")]
    pub extensions: ::prost::alloc::vec::Vec<Token2022Extension>,
    /// Metaplex Token Metadata associated with the mint.
    ///
    /// Only populated for legacy SPL Token mints that have an associated Metaplex
    /// metadata PDA (i.e. created via CreateSPLTokenMint with metadata provided).
    /// The metadata PDA is derived using the standard Metaplex seed convention:
    ///    \["metadata", METADATA_PROGRAM_ID, mint_pubkey\].
    ///
    /// For Token-2022 mints this field is always absent — metadata is instead
    /// returned inline via the extensions field.
    #[prost(message, optional, tag="4")]
    pub metaplex_metadata: ::core::option::Option<MetaplexTokenMetadata>,
}
/// Structured mint account information
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MintInfo {
    #[prost(string, tag="1")]
    pub mint_authority_pub_key: ::prost::alloc::string::String,
    #[prost(string, tag="2")]
    pub freeze_authority_pub_key: ::prost::alloc::string::String,
    #[prost(uint32, tag="3")]
    pub decimals: u32,
    #[prost(string, tag="4")]
    pub supply: ::prost::alloc::string::String,
    #[prost(bool, tag="5")]
    pub is_initialized: bool,
}
/// Request to create a Token-2022 holding account (Associated Token Account)
/// with optional extensions.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateToken2022HoldingAccountRequest {
    /// The public key of the account that will pay for account creation (signer).
    #[prost(string, tag="1")]
    pub payer_pub_key: ::prost::alloc::string::String,
    /// The public key of the wallet that will own the holding account
    /// (controls the token balance).
    #[prost(string, tag="2")]
    pub owner_pub_key: ::prost::alloc::string::String,
    /// The public key of the mint this holding account will hold tokens for.
    #[prost(string, tag="3")]
    pub mint_pub_key: ::prost::alloc::string::String,
    /// Ordered list of Token-2022 extensions to enable on the holding account.
    /// Extensions that are not included here during creation cannot be added
    /// later without reallocating the account.
    #[prost(message, repeated, tag="4")]
    pub extensions: ::prost::alloc::vec::Vec<Token2022HoldingAccountExtension>,
}
/// Response containing the complete instruction set for Token-2022 holding
/// account creation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateToken2022HoldingAccountResponse {
    /// Ordered instructions to include in a transaction.
    /// Includes ATA creation and optionally reallocate + extension-init
    /// instructions for each requested extension.
    #[prost(message, repeated, tag="1")]
    pub instructions: ::prost::alloc::vec::Vec<super::super::super::transaction::v1::SolanaInstruction>,
    /// Lamports required for the holding account to be rent-exempt.
    /// Accounts for the base token account size plus any requested extensions.
    #[prost(uint64, tag="2")]
    pub lamports: u64,
}
/// Request to create a legacy SPL Token holding account (Associated Token Account).
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateSplTokenHoldingAccountRequest {
    /// The public key of the account that will pay for account creation (signer).
    #[prost(string, tag="1")]
    pub payer_pub_key: ::prost::alloc::string::String,
    /// The public key of the wallet that will own the holding account
    /// (controls the token balance).
    #[prost(string, tag="2")]
    pub owner_pub_key: ::prost::alloc::string::String,
    /// The public key of the mint this holding account will hold tokens for.
    #[prost(string, tag="3")]
    pub mint_pub_key: ::prost::alloc::string::String,
}
/// Response containing the instruction set for SPL Token holding account creation.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct CreateSplTokenHoldingAccountResponse {
    /// A single ATA creation instruction.
    #[prost(message, repeated, tag="1")]
    pub instructions: ::prost::alloc::vec::Vec<super::super::super::transaction::v1::SolanaInstruction>,
    /// Lamports required for the holding account to be rent-exempt.
    #[prost(uint64, tag="2")]
    pub lamports: u64,
}
/// Request to mint tokens to a destination account.
///
/// The service retrieves the mint account on-chain to resolve the mint
/// authority, decimal precision, and owning token program — so callers do not
/// need to supply those values.
///
/// NOTE: Multi-sig mint authorities are not yet supported.
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MintRequest {
    /// Public key of the token mint to mint from.
    #[prost(string, tag="1")]
    pub mint_pub_key: ::prost::alloc::string::String,
    /// Public key of the **system account** (wallet) that owns the destination
    /// token account.  The Associated Token Account (ATA) is derived
    /// automatically from this owner and the mint.
    ///
    /// Do NOT pass the ATA address itself — pass the owner's system account.
    #[prost(string, tag="2")]
    pub destination_owner_pub_key: ::prost::alloc::string::String,
    /// Human-readable token amount as a decimal string.
    ///
    /// Express the amount in whole-token units (e.g. "1.5" for one-and-a-half
    /// tokens).  The service converts this to the on-chain base-unit
    /// representation using the mint's decimal precision.
    ///
    /// Examples (assuming 6 decimals):
    ///    "1.0"     → 1 000 000 base units
    ///    "0.5"     → 500 000 base units
    ///    "1000"    → 1 000 000 000 base units
    ///    "0.000001"→ 1 base unit
    #[prost(string, tag="4")]
    pub amount: ::prost::alloc::string::String,
}
/// Response containing Mint instruction
#[allow(clippy::derive_partial_eq_without_eq)]
#[derive(Clone, PartialEq, ::prost::Message)]
pub struct MintResponse {
    #[prost(message, optional, tag="1")]
    pub instruction: ::core::option::Option<super::super::super::transaction::v1::SolanaInstruction>,
}
include!("protochain.solana.program.token.v1.tonic.rs");
// @@protoc_insertion_point(module)